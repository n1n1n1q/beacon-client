CLIENT_ID=beacon-client-01
BEACON_INTERVAL_HOURS=1

SERVER_HTTP_BASE=http://localhost:8000
SERVER_WS_BASE=ws://localhost:8000

SERVER_TCP_HOST=localhost
SERVER_TCP_PORT=9000

SERVER_HTTP2_HOST=localhost
SERVER_HTTP2_PORT=8443

SERVER_HTTP3_HOST=localhost
SERVER_HTTP3_PORT=4433

SERVER_DNS_HOST=127.0.0.1
SERVER_DNS_PORT=5354
DNS_ZONE=alive.beacon.local

SERVER_DOH_HOST=127.0.0.1
SERVER_DOH_PORT=8043

SERVER_ICMP_HOST=127.0.0.1

SERVER_FTP_HOST=localhost
SERVER_FTP_PORT=2121
FTP_USER=beacon
FTP_PASSWORD=beacon

SERVER_IMAP_HOST=localhost
SERVER_IMAP_PORT=1143
IMAP_USER=beacon
IMAP_PASSWORD=beacon

SERVER_SMB_HOST=localhost
SERVER_SMB_PORT=1445
SMB_USER=beacon
SMB_PASSWORD=beacon
SMB_SHARE=BEACON

SERVER_MAPI_HOST=localhost
SERVER_MAPI_PORT=8007
MAPI_USER=beacon
MAPI_PASSWORD=beacon

# TCP, HTTP, HTTP2, HTTP3, DNS, DOH, ICMP, FTP, IMAP, MAPI, SMB, LDAP
ENABLED_CHANNELS=HTTP,TCP,HTTP2,HTTP3,DNS,ICMP,FTP
